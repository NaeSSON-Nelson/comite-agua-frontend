import { environment } from "src/environments/environment";

const URL_API = environment.apiURrl;

// URLS PUBLIC
export const URL_SIGNIN = `${URL_API}/auth/login`
export const URL_AUTH_REFRESH= `${URL_API}/auth/refresh`

// URLS PRIVATE

